<% content_for :title, "Editing friend" %>

<h1>Editing friend</h1>

<%= render "form", friend: @friend %>

<br>



<div class="d-flex">
  <%= link_to "Show", @friend, class:"btn btn-primary me-2" %>
  <%= button_to "Delete", @friend, method: :delete , form: { data: { turbo_confirm: 'Are you sure?' } }, class: "btn btn-danger me-2" %>
  <%= link_to "Back", friends_path, class:"btn btn-dark" %>
</div>
